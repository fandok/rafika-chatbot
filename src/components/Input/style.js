import { css } from 'emotion';

export const cssInputChip = css({
  border: '1px solid #000000',
  boxSizing: 'border-box',
  borderRadius: 8,
  marginRight: 13,
  minWidth: 117,
});

export const cssInputChipContent = css({
  color: '#000000',
  padding: 13,
  fontSize: 13,
});

export const cssInputContainer = css({
  padding: '16px 16px 24px 16px',
  display: 'flex',
  flexDirection: 'row',
  overflow: 'auto',
});
